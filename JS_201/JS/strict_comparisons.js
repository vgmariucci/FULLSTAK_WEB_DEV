console.log("Successfully loaded strict_comparisons.js")

let num = "0";

//compare if the num variable is exactly (strictly) equals the number 0
if(num === 0){
    // if the num variable is strictly equal to the number type and is equal to 0, then show this message
    console.log("The variable is a number type, and it is equal to  " + num);
}
else {
    // if the num variable is not a number type or is not equal to 0, then show this message
    console.log("The variable is not a number type!");
}