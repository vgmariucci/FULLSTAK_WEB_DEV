# Tuple are immutable, we cannot modify them, just count its index 
# and measure its length (i.e. the number of elements it contains)
foods = ('Pizza', 'Fish', 'Potato')

#printing the elements of the tuple defined above
for food in foods:
    print(food)
